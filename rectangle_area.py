#enter via keyboard the base and height of a rectangle, calculate and exibit its area
base = float(input('Enter the base of the rectangle: '))
height = float(input('Enter the height of the rectangle: '))
area = base * height
print('\nThe area of this rectangle is', area)